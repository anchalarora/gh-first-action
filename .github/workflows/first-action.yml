name : first workflow
on : workflow_dispatch
jobs:
  first-job :
    runs-on : ubuntu-latest
    steps:
      - name: Print greeting
        run : echo "Hello Anchal"
      - name : Print goodbye
        run : echo "Print Gooodbye"
  
